function select(p1, p2) {
  var sql = "SELECT * FROM students";
  return connection.query(sql);
};

function delete(p1, p2) {
  var sql = " * FROM students";
  return connection.query(sql);
};

function update(p1, p2) {
  var sql = " * FROM students";
  return connection.query(sql);
};
